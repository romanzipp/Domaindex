services:
  app:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      APP_SECRET: change-me-to-a-random-secret
      DB_DRIVER: sqlite
      DB_DSN: data/domaindex.db
      WHOIS_REFRESH_INTERVAL: 6h
      REGISTRATION_ENABLED: "true"
      # NOTIFICATION_URLS: pushover://shoutrrr:apiToken@userKey/
    restart: unless-stopped

